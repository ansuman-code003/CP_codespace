class Solution {
public:
    bool isMonotonic(vector<int>& nums) {
        int sz=nums.size();
        bool type=false; // true = inc; false = dec
        if(nums[0] > nums[sz-1]) type=true;
        for(int i=0;i<sz-1;i++){
            
        }

    }
};
